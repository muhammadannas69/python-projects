# Expense Tracker (Mini Project)

A simple command-line Expense Tracker built with Python. This project allows users to add, view, calculate, and delete expenses using a menu-driven interface.

## Features

- Add a new expense
- View all saved expenses
- Calculate the total of all expenses
- Delete an expense by its index
- Exit the program safely

## Exception Handling

This project uses `try-except-finally` to handle errors such as:

- Invalid menu option
- Invalid numeric input
- Negative or zero expense amount
- Empty expense list
- Invalid index while deleting an expense

## Concepts Used

- Python Lists
- Loops (`while`, `for`)
- Conditional Statements (`if`, `elif`, `else`)
- Exception Handling (`try`, `except`, `finally`)
- User Input
- List Methods (`append()`, `pop()`)
- Variables

## How to Run

1. Clone this repository.
2. Open the project in VS Code.
3. Run the Python file:

```bash
python expense_tracker.py
```

## Sample Menu

```text
1. Add Expense
2. View Expenses
3. Calculate Total
4. Delete Expense
5. Exit
```

## Author

**Muhammad Annas**  
BS Artificial Intelligence (7th Semester)